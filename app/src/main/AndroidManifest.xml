<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wua.mc.webuntisapp">

    <uses-sdk android:minSdkVersion="19" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.GlobalCalendar" >
            <intent-filter>
                <action android:name="com.wua.mc.webuntisapp.view.GlobalCalendar" />
            </intent-filter>
        </activity>

        <activity android:name=".view.PersonalCalendar" >
            <intent-filter>
                <action android:name="com.wua.mc.webuntisapp.view.PersonalCalendar" />
            </intent-filter>
        </activity>

        <service
            android:name=".presenter.CalendarPresenter"
            android:enabled="true" >
            <intent-filter>
                <action android:name="com.wua.mc.webuntisapp.presenter.CalendarPresenter" />
            </intent-filter>
        </service>

        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>